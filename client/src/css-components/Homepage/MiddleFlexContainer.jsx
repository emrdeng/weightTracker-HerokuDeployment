import {Container} from "@mui/material";
import {styled} from "@mui/system";

const MiddleFlexContainer = styled(Container)({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "35vh",
    width: "100%",
    overflow: "hidden",
})

export default MiddleFlexContainer;
