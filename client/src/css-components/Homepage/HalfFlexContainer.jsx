import {Container} from "@mui/material";
import {styled} from "@mui/system";

const HalfFlexContainer = styled(Container)({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "20vh",
    width: "100%",
    paddingTop: "60px",
})

export default HalfFlexContainer;