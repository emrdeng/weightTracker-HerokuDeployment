import {Box} from "@mui/material";
import {styled} from "@mui/system";

const SoloBox = styled(Box)({
    display: "inline-flex",
    justifyContent: "center",
    alignItems: "left",
    height: "80%",
    width: "30%",
    color: "white",
    backgroundColor: "rgba(99, 85, 92, 0.44)",
    paddingLeft: "40px",
    flexDirection: "column",
    flexWrap: "wrap",
    borderRadius: "20px"
})

export default SoloBox;
