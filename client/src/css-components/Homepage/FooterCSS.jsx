import {Container} from "@mui/material";
import {styled} from "@mui/system";

const FooterCSS = styled(Container)({
    display: "flex",
    justifyContent: "left",
    alignItems: "center",
    height: "7vh",
    width: "100%",
    backgroundColor: "#262a3b",
    overflow: "hidden",
    color: "white"
})

export default FooterCSS;